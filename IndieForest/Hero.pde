class Hero{
  PGraphics hero; //  contains main character
  /*
  @TODO: which methods do you need 
  return width
  return height 
  
  // because of the privacy issues
  current pixel color ? 
  set pixel color ? 
  
  */
  
  Hero(int w, int h){
    hero = createGraphics(w, h);
  } 
}